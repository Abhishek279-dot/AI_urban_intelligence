import React from "react";
import { ArrowRight, Camera, Cpu, Database, MapPinned, Server, ShieldCheck } from "lucide-react";
import SectionHeader from "../components/SectionHeader";
const steps=[[Camera,"Capture","Road-facing camera + GPS on public transport"],[Cpu,"Understand","Edge AI detects, tracks and classifies events"],[MapPinned,"Geo-tag","Attach latitude, longitude, timestamp and confidence"],[Server,"Deliver","FastAPI exposes clean REST endpoints"],[Database,"Intelligence","Store events for GIS, analytics and workflow"]];
export default function Platform(){return <main className="page"><SectionHeader eyebrow="THE PLATFORM" title="A mobile sensing network built into the fleet." text="TransitEye combines bus cameras, GPS and edge AI with a central GIS command center. The frontend stays API-ready: React communicates with FastAPI, never directly with MySQL."/><div className="architecture">{steps.map(([Icon,title,desc],i)=><React.Fragment key={title}><div className="arch-card"><span><Icon size={23}/></span><small>0{i+1}</small><h3>{title}</h3><p>{desc}</p></div>{i<steps.length-1&&<ArrowRight className="arch-arrow"/>}</React.Fragment>)}</div><div className="info-grid"><div className="info-card"><ShieldCheck/><h3>Edge-first processing</h3><p>Reduce bandwidth and storage by sending important metadata and evidence instead of continuous raw video.</p></div><div className="info-card"><MapPinned/><h3>GIS-ready events</h3><p>Every incident can be mapped, filtered and opened for review with location and confidence.</p></div><div className="info-card"><Cpu/><h3>AI pipeline</h3><p>YOLO-style detection, tracking and OCR can become structured events for the operational dashboard.</p></div></div></main>}
